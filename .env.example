# 复制为 .env.local 后按需修改（.env* 已 gitignore，勿提交密钥）

# 部署子路径（GitHub Pages 子目录时由 CI 注入，本地一般不用）
# VITE_BASE_PATH=/react-antd-log/

# 使用 Hash 路由（GitHub Pages 推荐 true，本地默认 History）
# VITE_HASH_ROUTER=true

# 可选：覆盖构建版本展示（默认取 package.json version）
# VITE_APP_VERSION=0.0.0
