---
description: Figma/FigJam 经 MCP 默认只读；读稿失败不得臆造 UI；写入仅用户当条指令明确授权
alwaysApply: true
---

# Figma 设计稿保护（MCP）

## 总原则

通过 MCP 对接 Figma / FigJam 时，**默认只读、不改动文件**；实现界面须以可读到的稿面或用户另行提供的依据为准，**不得用未经验稿的臆测界面冒充「按稿实现」**。

## 禁止写入

**不得**使用会**创建、修改、删除**稿面内容的 MCP 能力，包括但不限于：TalkToFigma、Figma 官方 MCP、Chrome DevTools 等场景中针对 Figma 的自动化写入。

**典型禁止项**：增删改节点或画板；改填充、描边、圆角、文本、布局、间距、对齐；注释、实例覆盖、反应；向 Figma **写回**设计、`generate_figma_design`、FigJam **生成图表**。**批量导出**若会覆盖稿面或依赖写入流程，视为写入，**默认不做**；仅当用户在同一条指令中明确要求时再执行。

## 允许只读

**可以**只读获取：当前文档/选中、元数据、`get_design_context`、截图、变量定义等，用于**对照写代码**。只读结果**不得**再驱动任何写入类 MCP 操作。

## 未连接或读稿失败

任务**依赖** Figma 对照，但出现 **MCP 未连接、调用失败、超时、无有效稿面信息** 时：

- **禁止**凭想象或「通用模板」补全、重画、替代 UI。
- **必须**向用户说明无法读稿，请检查 MCP / 插件 / 网络后重试；或请用户提供**截图、标注、导出说明、已定稿规格**等替代依据后再继续。

## 写入的唯一例外

仅当用户在**同一条明确指令**中要求执行某项**具体**的 Figma 写入操作时，方可使用对应写入能力；除此之外**一律不调用**写入类 Figma MCP。
